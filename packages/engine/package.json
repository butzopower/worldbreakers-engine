{
  "name": "@worldbreakers/engine",
  "version": "0.1.0",
  "description": "Pure state-machine game engine for Worldbreakers card game",
  "type": "module",
  "main": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "scripts": {
    "build": "tsdown src/index.ts --format esm --out-dir dist --dts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "tsdown": "^0.20.3",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
